% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_active_field.R
\name{make_active_field_wrapper}
\alias{make_active_field_wrapper}
\alias{make_active_field}
\title{Build a named list of read-only active bindings for an R6 fields}
\usage{
make_active_field_wrapper(field_names)

make_active_field(field_name)
}
\arguments{
\item{field_names}{A character vector of private field or method names.}

\item{field_name}{A single character string naming the active field.
The corresponding private field or method is expected at
\verb{private$.<field_name>} following the common R6 convention.}
}
\value{
\itemize{
\item \code{make_active_field_wrapper()} returns a named list of active-binding
functions. One per element of\code{field_names} and with names equal to
\code{field_names}.
\item \code{make_active_field()} returns a function with a one optional argument
\code{value}. Called with no argument (e.g. \code{obj$field}) it returns
\verb{private$.<field_name>}. Called with a value (e.g. \code{obj$field <- x}) it
stops with a "read-only" error.
}
}
\description{
Convenience wrapper that applies \code{\link[=make_active_field]{make_active_field()}} to each of field names
and returns them as a named list, ready to be dropped into the \code{active}
argument of an \link[R6:R6Class]{R6::R6Class} definition.
}
\details{
\code{\link[=make_active_field]{make_active_field()}} generates a function suitable for use as an active
binding in an \link[R6:R6Class]{R6::R6Class} definition. The returned function exposes the
private field \verb{private$.<field_name>} for reading and raises an error on
any attempt to assign to it.
}
\examples{
\dontrun{
PointClass <- R6::R6Class(
  "PointClass",
  private = list(.x = 0),
  active  = list(x = make_active_field("x"))
)
p <- PointClass$new()
p$x # reads 'private$.x' value
p$x <- 5 # Error: 'x' is read-only

RecordClass <- R6::R6Class(
  "RecordClass",
  private = list(.id = 109, .created = "2021-09-14"),
  active  = make_active_field_wrapper(c("id", "created"))
)
r <- Record$new()
r$id # reads 'private$.id' value
r$created # reads 'private$.created' value
r$id <- 10 # Error: 'id' is read-only
r$created <- "2023-01-01" # Error: 'created' is read-only
}
}
